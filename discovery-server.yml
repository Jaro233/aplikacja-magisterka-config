server:
  port: 8761

eureka:
  instance:
    hostname: ${DISCOVERY_SERVER_URL:localhost}
  # standalone mode
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${DISCOVERY_SERVER_URL:localhost}:${DISCOVERY_SERVER_PORT:8761}/eureka/
      
