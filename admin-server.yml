server:
  port: 9090

---
spring:
  config:
    activate:
      on-profile: docker
eureka:
  client:
    serviceUrl:
      defaultZone: http://${DISCOVERY_SERVER_URL:discovery-server}:${DISCOVERY_SERVER_PORT:8761}/eureka/
